# MongoDB Atlas Connection String
# Lấy từ: Atlas Console > Connect > Drivers > Copy string
MONGO_URI=mongodb+srv://atlantisdeepslumbercontacthelp_db_user:bdDh1g6kg3zm8jI2@atlantisdeepslumber.cqelqkv.mongodb.net/

# JWT Secret — Tạo ngẫu nhiên: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_64_char_random_hex_string

# Admin Secret Key for Frontend Dashboard Access
ADMIN_SECRET_KEY=adminatlantis123

# Gmail SMTP — Dùng App Password (không phải mật khẩu thường)
# Tạo App Password: myaccount.google.com > Security > 2-Step Verification > App Passwords
EMAIL_USER=atlantisdeepslumber.contact.help@gmail.com
EMAIL_PASS=rpar pvvf ztra ituh

# Tên hiển thị trong email gửi đi
EMAIL_FROM_NAME=Atlantis Deep Slumber

# Port backend (mở port này trên VPS firewall)
PORT=3000

# URL công khai của backend (dùng cho thông báo email)
# Ví dụ: http://123.456.789.0:3000  hoặc  https://api.yourdomain.com
BACKEND_URL=http://165.99.14.40:3000
